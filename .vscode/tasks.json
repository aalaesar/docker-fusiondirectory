{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build container",
      "type": "shell",
      "command": "docker build --no-cache --build-arg fd_version=1.3 -t fusion-directory:1.3 .",
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}